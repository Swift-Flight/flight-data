.build/
.swiftpm/
*.xcodeproj
.DS_Store
*.o
*.swiftmodule
# Package.resolved IS committed: this repository's own CI must build the
# exact dependency versions that were verified locally. SwiftPM ignores a
# dependency's resolved file, so this never constrains consumers.
.vscode/
